@using Agence_Practical_Test.Models;
@using Agence_Practical_Test.DTO;
@model List<ConDesemConsultorRel>
    @foreach (var item in Model)
    {
<div>
    @Html.Partial("Con_desem_consultor_rel",item);
</div>
    }
